event: extensions
op: and
rules:
  - op: is
    path: routing/hostname
    value: ext-secureannex
  - op: is
    path: event/results/extensions/?/visibility
    value: unlisted
- action: report
  name: Secure Annex - unlisted browser extension detected
